"""Manual-testing helpers, run as modules so that `app` is importable.

`python scripts/x.py` puts `scripts/` on the path and not the working
directory, so these are run with `-m`: `uv run python -m scripts.x`.
"""
