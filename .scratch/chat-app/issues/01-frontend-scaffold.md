# 01 — Frontend scaffold

**What to build:** A running React app (Vite) with the tooling decided in the design session wired up — TanStack Query configured, a routing shell, no real pages yet beyond a placeholder. Proves the toolchain works before any feature UI is built on top of it.

**Blocked by:** None — can start immediately.

**Status:** done

- [x] `frontend/` scaffolded with Vite + React + TypeScript
- [x] TanStack Query provider configured
- [x] Router in place (e.g. React Router) with a placeholder route
- [x] `frontend/README.md` documents how to run it locally
- [x] App builds and runs (`npm run dev`) without errors
